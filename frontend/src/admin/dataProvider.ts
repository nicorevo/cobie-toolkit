import { supabase } from '../lib/supabase/client';

// Preferred path: use ra-supabase if it matches project requirements.
// This file is intentionally a placeholder for the Frontend Agent.

// Example expected shape:
// import { supabaseDataProvider } from 'ra-supabase';
// export const dataProvider = supabaseDataProvider({ instanceUrl, apiKey, supabaseClient: supabase });

export const dataProvider = undefined as never;
void supabase;
